from classes.simulation import run


def plot_convergence(sim):
    return


if __name__ == '__main__':
    total_nodes_list = [100, 100, 100]
    polynomial_list = [2, 4, 6]
    results = run(total_nodes_list, polynomial_list)
